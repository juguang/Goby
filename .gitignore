# Dependencies
node_modules/

# Local-only design docs / prototypes (contain internal API endpoints)
GOBY_DESIGN.md
test.html
test_副本.html

# Playwright MCP session logs
.playwright-mcp/

# GSD planning artifacts (kept local — project decisions/workflow not for upstream)
.planning/

# Local Claude Code project instructions (per-developer, not for upstream)
CLAUDE.md

# Release notes — 本地保留，不上传，Release notes 只放 GitHub Release
RELEASES.md
